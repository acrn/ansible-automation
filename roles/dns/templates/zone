; {{ ansible_managed }}
$TTL 1D
@	IN SOA	ns1.{{ dns_domain }}. admin.{{ dns_domain }}. (
					4	; serial
					1D	; refresh
					1H	; retry
					1W	; expire
					3H )	; minimum

; name servers - NS records
	IN	NS	ns1.{{ dns_domain }}.

; name servers - A records
ns1.{{ dns_domain }}.	IN	A	192.168.1.67

; 10.128.0.0/16 - A records
{% for ip, hostnames in dns_a_records.items() %}
{% for hostname in hostnames %}
{{ hostname }}.{{ dns_domain }}.	IN	A	{{ ip }}
{% endfor %}
{% endfor %}
